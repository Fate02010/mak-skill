# Step 6：需求对照审视 + 改进建议迭代

所有原型图生成完毕后，**先强制执行需求对照审视（阶段 6-0），再进入改进建议环节**。

---

## 阶段 6-0：需求对照审视（强制，不可跳过）

> 目标：逐页对照需求文档与原始资料，检查六个维度，输出审视报告并等待用户确认后修复。
> 通用：Read `SKILL_DIR/steps/step5-acceptance-rules.md` 获取 A/B/C 三类验收规则。

### 准备工作

1. 读取 `WORK_DIR/详细需求文档.md`，提取"原型图清单"和每个页面的功能描述、字段列表
2. 读取 `WORK_DIR/原型任务清单.md`，获取已生成的页面列表
3. 扫描已生成文件：
   - **HTML 模式**：`Glob WORK_DIR/prototypes/*.html`
   - **draw.io 模式**：读取 `WORK_DIR/prototypes/[产品名称].drawio`，列出所有 `<diagram name>` 和 swimlane

### 六维度审视

**维度一：界面完整性**

对照"原型图清单"中每一行，逐一确认对应界面是否已生成：
- HTML 模式：对应 `.html` 文件是否存在
- draw.io 模式：对应 swimlane 是否在某个 diagram 中存在
- 记录所有缺失页面

**维度二：字段/元素完整性**（逐页检查）

对每个已生成页面，读取其内容，对照需求文档中该页面的字段列表/功能描述，检查：
- 必填字段是否全部存在（如表单的每个输入项）
- 关键操作按钮是否存在（如提交、取消、查看、编辑、删除）
- 列表页的列标题是否完整（是否缺少某列）
- 状态标签/枚举值是否体现（如订单状态、商品分类）
- 关键提示文案是否有（如空态引导语、错误提示位置）
- **空态覆盖**：列表页、首页、Dashboard 类页面是否有空态展示（无数据时的引导文案 + 引导操作按钮）；缺失则标记
- **异常态覆盖**：核心页面（列表、详情、表单提交）是否有异常态展示（加载失败/网络错误提示 + 重试按钮）；缺失则标记
- **处理中态**：表单提交、支付等关键操作是否有处理中态（按钮 disabled + 文字变"处理中..."）；缺失则标记

**维度三：排版合理性**

- **通用**：页面是否有清晰的视觉主次层级，主操作是否突出
- **HTML 模式**：导航/标题/内容/操作按钮区域划分是否清晰；是否有未对齐的组件
- **draw.io 模式**：UI 区内是否混入了标注文字；标注区是否有内容越界进入 UI 区；同一模块页面 swimlane 宽高是否一致
- **骨架隔离检查**（通用）：登录页、注册页、错误页、支付结果页等独立页面，是否错误包含了侧边栏、顶部业务导航或面包屑；发现则标记为排版错误
- **多模式互斥检查**（通用）：需求中含"支持 X 和 Y 两种方式"的页面，是否将两种方式字段同时平铺展示（而非 Tab 切换）；发现则标记为布局错误

**维度四：内容真实性**

逐页检查，发现以下任意情况标记为内容错误：
- 出现占位文字：`功能待定`、`这里是内容`、`示例文字`、`文字N`、`按钮N`、`Lorem ipsum`、`待补充`、`暂无`（无引导操作时）
- 表格数据行使用`数据1/数据2`、`张三/李四`（虚构姓名可接受，但字段值不能是序号）
- 字段 label 使用`字段1`、`输入框A`等非业务名称

**维度五：视觉结构（规则 B3/C1/C2/C3/C4）**

按 `step5-acceptance-rules.md` 中 B3 和 C 类规则逐页检查：

- **B3 主区突出**：计算主内容区面积占比（表格区/字段卡片/键值对区），占比 < 30% 标记为主区不突出
- **C1 说明框过多**：UI 区内纯文字说明元素（value > 30 字符的 text 元素，非 label/title）≥ 5 个 → 标记
- **C2 大段文字占主体**：UI 区内单个 mxCell value > 80 字符（标注区除外）→ 标记
- **C3 页面像文档/PPT**：统计交互组件占比（input/btn/select/tag），交互组件 < 10% 或文字组件 > 70% → 标记为"像文档"
- **C4 内容同权**：检查按钮是否只有一种样式（全是 btn_primary），字号是否只有一种 → 标记为"无视觉层级"

**维度六：主流程闭环复核（规则 A5）**

复核 Step 5-5 中的主流程闭环校验结果，重点检查 Step 5-5 修复后是否引入新的断点：
- 补充的按钮是否有正确的 tooltip/href 跳转目标
- 补充的页面是否能从其他页面到达
- 操作成功/失败后是否有反馈（Toast/错误提示）

### 输出审视报告

```
=== 原型需求对照审视报告 ===

【维度一：界面完整性】
✅ 所有 N 个页面均已生成
⚠️ 缺失页面（M 个）：
  - [页面名]（需求文档章节：§X.X）
  - [页面名]（需求文档章节：§X.X）

【维度二：字段/元素完整性】
✅ [页面名]：字段完整
⚠️ [页面名]：缺失以下元素
  - [字段名/按钮名]（需求文档 §X.X 要求）
  - [字段名/按钮名]
⚠️ [页面名]：缺失状态展示
  - 空态：无数据时缺少引导文案和操作按钮
  - 异常态：加载失败场景未绘制
  - 处理中态：提交按钮缺少 disabled 状态

【维度三：排版合理性】
✅ 整体布局合理
⚠️ [页面名]：[问题描述，如"标注文字覆盖了输入框区域"]
⚠️ [页面名]：骨架隔离违规 — 登录页出现侧边导航，需去除
⚠️ [页面名]：多模式堆叠 — 两种登录方式字段同时展示，需改为 Tab 切换

【维度四：内容真实性】
✅ 所有页面使用真实业务数据
⚠️ [页面名]：发现占位文字 — "[具体文字]"，需替换为真实业务内容

【维度五：视觉结构】
✅ [页面名]：主区面积占比 62%，交互组件占比 28%，视觉层级清晰
⚠️ [页面名]：C1 说明框过多 — UI 区有 6 个纯文字说明元素，建议移至标注区
⚠️ [页面名]：C3 像文档 — 交互组件占比 8%，文字组件占比 75%，需补充交互组件
⚠️ [页面名]：C4 内容同权 — 所有按钮均为 btn_primary，需区分主次

【维度六：主流程闭环】
✅ [流程名]：闭环完整
⚠️ [流程名]：[缺失环节描述]

=== 发现问题共 X 处，建议修复 ===
是否立即修复？（"全部修复" / "修复1,3" / "跳过进入改进建议"）
```

### draw.io 模式：HTML 对比（强制执行）

> **draw.io 模式下，阶段6-0 开始时必须先执行 HTML 对比，再做六维度审视。** HTML 是高保真参照基准，用于发现 draw.io 中肉眼难以核实的缺失元素和布局偏差。
>
> **强制对比范围（以下页面必须生成 HTML 对比稿）：**
> 1. 所有移动端主导航页（需求文档 §3.5 中标注的底部 Tab Bar 页面）
> 2. 首页 / Dashboard 类页面
> 3. 任何在需求文档中字段规格 ≥ 5 个字段的表单页或详情页
> 4. 审视过程中发现 ≥ 1 个问题的页面（在审视中追加生成）
>
> 对比完成后，再进入六维度审视。

触发时执行以下流程：

**Step A：生成对比用 HTML 临时稿**

为存疑页面生成轻量 HTML 版本，保存到 `WORK_DIR/review_tmp/[页面名].html`：
- 读取 `SKILL_DIR/steps/html-spec.md` 获取 HTML 规范
- 基于需求文档同一章节生成该页面的 HTML 版本
- 输出只需覆盖核心功能，不追求完整交互效果
- 禁止在控制台输出 HTML 正文，只输出：`✅ 对比稿 [页面名].html 生成完毕`

**Step B：对比差异，列出 draw.io 问题清单**

逐项对比 HTML 稿与 draw.io 对应 swimlane，记录差异：

```
=== [页面名] HTML vs draw.io 对比 ===

字段/元素差异：
  ✅ [字段名]：两者一致
  ❌ [字段名]：HTML 有，draw.io 缺失
  ❌ [字段名]：HTML 有，draw.io 缺失

排版差异：
  ⚠️ HTML 中 [区域A] 在顶部导航下方，draw.io 中位置偏移约 [N]px
  ⚠️ HTML 中按钮组水平排列，draw.io 中为垂直堆叠

结论：draw.io 需修正 X 处
```

**Step C：以 HTML 稿为参照修正 draw.io**

按对比清单，用 `Edit` 工具精确修改 `[产品名称].drawio` 对应 swimlane：
- 补充缺失的 `<mxCell>` 元素（字段/按钮），坐标参照 HTML 布局折算
- 调整位置偏差的组件坐标
- 修复排版结构（水平/垂直排列、对齐方式）

**Step D：清理临时文件**

修复完成后删除 `WORK_DIR/review_tmp/` 目录下本次生成的所有 HTML 临时稿。

---

### 执行修复（常规问题）

用户确认后，逐项修复。**draw.io 模式修复优先级：Edit page_spec → 重跑渲染 Agent（禁止直接 patch XML，除非是微小坐标调整）**：

- **缺失页面**：参照 Step 5 agent prompt 模板，单独启动 Agent 生成该页面；draw.io 模式先在 page_spec 中补充对应 swimlane，再重跑渲染 Agent，最后追加到 `.drawio` 文件
- **缺失字段/按钮**：draw.io 模式：用 `Edit` 修改对应 `page_spec_*.md`（在元素列表中追加行）→ 重跑该模块渲染 Agent；HTML 模式：直接 Edit `.html` 文件
- **排版/坐标问题**：draw.io 模式：修改 page_spec 中对应元素的 x/y/width/height → 重跑渲染 Agent；若只是 1-2 个元素的微调（< 3 个 mxCell），可直接 Edit `.drawio` XML
- 每次修复后更新 `执行状态.md` 的"Spec 版本记录"表格，记录版本号、变更原因和修改摘要

修复完成后，将审视结果追加到 `WORK_DIR/执行状态.md` 的 Step 6 迭代记录：

```
| 审视轮 | 发现 X 处问题，HTML对比 Y 页，已修复 Z 处 |
```

---

## 阶段 6-1：自评与建议生成

> **🚫 强制两轮迭代规则：原型初次完成后必须自动执行第 1、2 轮迭代，不得询问用户是否需要改进，直接生成建议并执行改进。第 3 轮起转为可选模式，等待用户指令。**

完成所有原型后，从以下维度审视已生成的原型，列出 **3-5 条具体的改进建议**：

**审视维度：**
- **流程完整性**：是否有用户路径无法走通？是否有功能模块缺少关键状态（如加载失败、权限不足）？
- **交互细节**：是否有可以更流畅的过渡动效？表单校验是否足够友好？
- **内容真实性**：示例数据是否贴近真实业务场景？空状态是否有引导文案？
- **竞品对标**：竞品分析报告中提到的差异化亮点是否都在原型中体现？
- **边缘场景**：错误页、超时页、无权限页等异常状态是否覆盖？

**建议格式：**

```
=== 原型改进建议（第 N / 5 轮）===
[第 1、2 轮附注：强制迭代轮，自动执行所有改进项]

我在审视生成的原型后，发现以下可以进一步完善的方向：

1. [建议标题]
   问题：[当前原型的不足之处]
   改进：[具体怎么改]
   影响页面：[HTML模式：哪些.html文件 / draw.io模式：哪个diagram的哪个swimlane]

2. [建议标题]
   ...
```

## 阶段 6-2：执行改进

**第 1、2 轮（强制迭代）：**
- 无需等待用户确认，列出建议后**立即自动执行全部改进项**
- 改进完成后输出本轮摘要，直接进入下一轮

**第 3 轮起（可选迭代）：**
- 列出改进建议后，等待用户选择：

```
=== 原型改进建议（第 N / 5 轮）===

...建议列表...

是否需要继续改进？请告诉我：
- 回复"全部改"：按照以上所有建议改进
- 回复"改1,3"（数字）：只改指定建议
- 回复"不用了"：结束，保持现状
- 或者直接描述你有其他想法
```

**执行步骤（每轮共用）：**

1. 对每个改进项，直接编辑对应的文件
2. 如果改进引入了新的需求决策，同步更新 `详细需求文档.md`
3. 在 `WORK_DIR/执行状态.md` 的"Step 6 迭代记录"中追加本轮摘要（轮次 + 改进内容摘要）
4. 当前迭代轮次 < 上限时，**再次进入阶段 6-1**，生成新一轮改进建议

**迭代上限：最多 5 轮**

**循环终止条件（满足任意一条即终止）：**
- 已完成强制两轮，且用户明确表示满意（回复"不用了"/"好了"/"完成"等）
- 连续两轮建议都没有实质性改进点（第 3 轮后）
- 用户主动中止（第 3 轮后）
- **已达到第 5 轮上限**：主动告知用户并停止循环：

```
已完成第 5 轮改进（迭代上限）。
如需继续优化，请重新触发 Step 6，或在下次会话中继续。
```

将 `执行状态.md` 整体状态更新为"已完成"。

> **改进原则：** 每轮改进要聚焦、可执行，不要提出过于笼统的建议（如"整体优化一下"）。每条建议应指向具体页面的具体问题，让用户能快速判断是否值得改。
