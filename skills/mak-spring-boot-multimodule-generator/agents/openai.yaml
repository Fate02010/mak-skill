interface:
  display_name: "Spring Boot Multi-module Generator"
  short_description: "Create or add Maven modules for Spring Boot projects (explicit invocation only)."
  default_prompt: "Use $spring-boot-multimodule-generator to scaffold a Spring Boot Maven multi-module project or add modules via a step-by-step wizard."

policy:
  allow_implicit_invocation: false
